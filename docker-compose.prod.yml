version: '3.7'
services:
  app:
    container_name: textsite_app
    image: php:7.4.0RC4-fpm
    volumes:
      - .:/var/www:delegated
    ports:
      - "9005:9000"
    networks:
      - net
networks:
  net:
    driver: "bridge"
